{
  "name": "nurov-marketplace",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix backend install --include=dev && npm --prefix backend run build && npm --prefix backend run prisma:deploy",
    "start": "node backend/dist/index.js"
  }
}
